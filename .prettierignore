node_modules
pnpm-lock.yaml
.output
.nitro
.tanstack

# generated by tanstack router
src/routeTree.gen.ts

# subagent-driven-development 작업 디렉터리 (git-ignored 스크래치).
# 여기 파일이 pnpm format:check를 깨면 안 된다 — 저장소 코드가 아니다.
.superpowers
